import { ResolvedValue } from "./ResolvedValue";
export declare class ResolvedValueWithId extends ResolvedValue {
  readonly id: string;
  private readonly value;
  constructor(id: string, value: ResolvedValue);
  asBoolean(): boolean;
  asNumber(): number;
  asText(): string;
  hasValue(): boolean;
  equals(other: ResolvedValue): boolean;
}
