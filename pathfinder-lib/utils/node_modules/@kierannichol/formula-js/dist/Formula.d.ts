import { Resolvable } from "./Resolvable";
export declare class Formula {
  private static Parser;
  static parse(formula: string | Resolvable): Resolvable;
  static optimize(formula: string): string;
  private static noneIfEmpty;
  private static variableFn;
  private static minFn;
  private static maxFn;
  private static sumFn;
  private static addFn;
}
