import { ResolvedValue } from "./ResolvedValue";
export declare class ResolvedRollValue implements ResolvedValue {
  private readonly count;
  private readonly sides;
  constructor(count: number, sides: number);
  asText(): string;
  asNumber(): number;
  asBoolean(): boolean;
  asList(): ResolvedValue[];
  equals(other: ResolvedValue): boolean;
  toString(): string;
  map(fn: (value: ResolvedValue) => ResolvedValue): ResolvedValue;
  hasValue(): boolean;
}
