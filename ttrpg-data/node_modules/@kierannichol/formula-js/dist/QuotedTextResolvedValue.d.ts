import { ResolvedValue } from "./ResolvedValue";
export declare class QuotedTextResolvedValue implements ResolvedValue {
  private readonly value;
  private readonly prefix;
  private readonly suffix;
  constructor(value: ResolvedValue, prefix: string, suffix: string);
  asBoolean(): boolean;
  asNumber(): number;
  asText(): string;
  asList(): ResolvedValue[];
  asQuotedText(): string;
  map(fn: (value: ResolvedValue) => ResolvedValue): ResolvedValue;
  equals(other: ResolvedValue): boolean;
  hasValue(): boolean;
}
