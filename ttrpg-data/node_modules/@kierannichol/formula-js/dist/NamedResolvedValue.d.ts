import { ResolvedValue } from "./ResolvedValue";
export declare class NamedResolvedValue implements ResolvedValue {
  private readonly value;
  private readonly name;
  constructor(value: ResolvedValue, name: string);
  asBoolean(): boolean;
  asNumber(): number;
  asText(): string;
  asList(): ResolvedValue[];
  asName(): string;
  map(fn: (value: ResolvedValue) => ResolvedValue): ResolvedValue;
  equals(other: ResolvedValue): boolean;
  hasValue(): boolean;
}
