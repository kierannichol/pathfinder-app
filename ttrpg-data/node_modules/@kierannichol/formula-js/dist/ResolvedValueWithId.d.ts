import { ResolvedValue } from "./ResolvedValue";
export declare class ResolvedValueWithId implements ResolvedValue {
  readonly id: string;
  private readonly value;
  constructor(id: string, value: ResolvedValue);
  asBoolean(): boolean;
  asNumber(): number;
  asText(): string;
  asList(): ResolvedValue[];
  map(fn: (value: ResolvedValue) => ResolvedValue): ResolvedValue;
  hasValue(): boolean;
  equals(other: ResolvedValue): boolean;
}
