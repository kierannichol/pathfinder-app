import { ResolvedValue } from "./ResolvedValue";
export declare class ResolvedListValue implements ResolvedValue {
  private readonly values;
  constructor(values: ResolvedValue[]);
  asText(): string;
  asNumber(): number;
  asBoolean(): boolean;
  asList(): ResolvedValue[];
  equals(other: ResolvedValue): boolean;
  toString(): string;
  map(fn: (value: ResolvedValue) => ResolvedValue): ResolvedValue;
  hasValue(): boolean;
  private latest;
}
